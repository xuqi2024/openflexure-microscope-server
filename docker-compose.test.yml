version: '3.8'
services:
  microscope-server:
    build: .
    healthcheck:
      test: curl --fail http://localhost:5000/ || exit 1
      interval: 5s
      timeout: 3s
      retries: 3
      start_period: 20s
    networks:
      - test_net

networks:
  test_net:
    driver: bridge
