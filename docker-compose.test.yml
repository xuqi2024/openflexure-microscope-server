version: '3.8'
services:
  microscope-server:
    build: .
    ports:
      - "5000:5000"
    healthcheck:
      test: curl --fail http://localhost:5000/ || exit 1
      interval: 5s
      timeout: 3s
      retries: 3
      start_period: 20s # Give everything time to boot.
